{
  "id": "wikipedia-search",
  "domain": "wikipedia",
  "action": "search",
  "runtime": "node",
  "description": "Search a Wikipedia language edition by keyword and return ranked article results with titles, snippets, timestamps, and URLs. Uses the public MediaWiki Action API; no login required.",
  "parameters": [
    {
      "name": "query",
      "description": "Search keyword.",
      "required": true
    },
    {
      "name": "limit",
      "description": "Number of results to return (1-50).",
      "required": false,
      "default": "10"
    },
    {
      "name": "offset",
      "description": "Pagination offset (skip N results).",
      "required": false,
      "default": "0"
    },
    {
      "name": "language",
      "description": "MediaWiki language edition code (e.g. zh, en, ja).",
      "required": false,
      "default": "zh"
    }
  ],
  "requiresBrowser": false,
  "authRequired": "not-required"
}